name: deploy

on:
  push:
    branches: [tailwind-refactor]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test thing
        run: |
          git fetch
          git checkout main
